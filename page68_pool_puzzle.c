void find_track(char search_for[]) {
	int i;
	for (i = 0; i < 5; i++) {
		if (strstr(tracks[i], ............ ))
			printf("Trach %i: '%s'\n", i, search_for );
	}
}

/*
 * my
 * tracks[i]
 * strstr
 * search_for
 * "Sinatra"
 * way
 * i
 * tracks[i]
 */

my


"Sinatra"
way
i
tracks[i]
